extension API {
    enum Keys {
        /// Obviously, this shouldn't be stored in a constant like this. I'm just keeping things simple here.
        static let `default` = "123"
        static let fdj = "50130162"   /// Didn't seem to be working
    }
}
